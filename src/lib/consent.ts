/**
 * Запасное значение версии согласия.
 *
 * Основное живёт в документе /consent в админке: текст правится там, и
 * версия обязана меняться вместе с текстом. Константа используется, только
 * если документа в базе ещё нет — на пустой базе до прогона сида.
 *
 * Экспортируется отсюда, а не из submitLead.ts: в файле с 'use server'
 * можно экспортировать только async-функции.
 */
export const CONSENT_VERSION_FALLBACK = '2026-08-27'
