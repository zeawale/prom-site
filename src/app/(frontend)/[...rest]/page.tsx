import { notFound } from 'next/navigation'

/**
 * Ловушка для адресов, не совпавших ни с одним маршрутом сайта.
 *
 * Нужна, чтобы такие адреса получали НАШУ 404 (not-found.tsx этой группы)
 * с шапкой и футером, а не заглушку Next. Вложенные маршруты — /admin,
 * /api, страницы группы (frontend) — конкретнее, чем catch-all, и в неё
 * не попадают.
 *
 * Страница динамическая по определению: список «всех остальных адресов»
 * заранее не собрать. Ответ — честный 404 без стриминга, поисковики
 * видят код, а не 200 с текстом «не найдено».
 */
export default function CatchAll() {
  notFound()
}
